{
  "extends": "../../tsconfig.base.json",

  "compilerOptions": {
    /* ---------- Çıktı klasörü ---------- */
    "outDir": "dist",
    "tsBuildInfoFile": "dist/tsconfig.app.tsbuildinfo",

    /* ---------- React & DOM ---------- */
    "jsx": "react-jsx",
    "lib": [
      "dom",                 // mevcut
      "dom.iterable",        // 🆕  fetch/Iterator API'leri
      "es2022"               // 🆕  Promise.withResolvers vb.
    ],
    "types": [
      "node",                                     
      "@nx/react/typings/cssmodule.d.ts",
      "@nx/react/typings/image.d.ts",
      "vite/client"
    ],

    /* ---------- Modül sistemi ---------- */
    "rootDir": "src",
    "module": "esnext",
    "moduleResolution": "bundler",   // Vite önerisi
    "allowImportingTsExtensions": true,  // 🆕  .tsx uzantı uyarılarını susturur

    /* ---------- Sıkı kontroller ---------- */
    "skipLibCheck": true             // base’den miras, ikincil hataları önler
  },

  /* ---------- Test & build dışı dosyalar ---------- */
  "exclude": [
    "out-tsc",
    "dist",

    "src/**/*.spec.{ts,tsx,js,jsx}",
    "src/**/*.test.{ts,tsx,js,jsx}",

    "vite.config.{ts,mts}",
    "vitest.config.{ts,mts}",
    "eslint.config.{js,cjs,mjs}"
  ],

  /* ---------- Dahil edilen kaynaklar ---------- */
  "include": ["src/**/*.{ts,tsx,js,jsx}"]
}